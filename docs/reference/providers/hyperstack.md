# Hyperstack

`sky.Hyperstack` creates an account descriptor for the `hyperstack` provider kind. `region`, image, networking, object-storage, and timeout settings are account configuration.

```python
import skyward as sky

provider = sky.Hyperstack(name="production", region="CANADA-1")
```

::: skyward.Hyperstack
