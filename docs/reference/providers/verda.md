# Verda

`sky.Verda` creates an account descriptor for the `verda` provider kind. Pass the client credentials explicitly or use `VERDA_CLIENT_ID` and `VERDA_CLIENT_SECRET`.

```python
import skyward as sky

provider = sky.Verda(name="finland", region="FIN-01")
```

::: skyward.Verda
